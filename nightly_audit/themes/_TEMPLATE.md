# テーマ: <name>

> このファイルが「監査の中身」。driver.md (挙動) はこれを読んで動く。
> 壁打ちで増やす/直すのは基本このファイル群だけ。

## 対象リポ
- <例: project_rf_rover, acsl_infra>  (このホストに無いものは自動でスキップされる)

## 観点 (チェックリスト)
- [ ] <何を見るか。具体的に。例: 「config の値がコードにハードコードされていないか」>
- [ ]

## 「指摘」とみなす条件
- <誤検知を減らすための明確な線引き。例: 「実際にパスが存在せず、かつ config 経由で
  渡せるのに直書きされているもの」。曖昧なもの・設計判断が要るものは指摘にしない>

## 自動修正してよい範囲
- <機械的に直せる範囲だけ許可。例: 「ドキュメント中のパス追従」「明白な typo」。
  設計変更・API 変更・挙動が変わる修正は PR にせずレポートの「要相談」へ>

## 検証方法
- <PR を出す前にどう確かめるか。例: py_compile / bash -n / grep で残存ゼロ確認 /
  colcon build --packages-select <pkg> / dup <backend> smoke>
